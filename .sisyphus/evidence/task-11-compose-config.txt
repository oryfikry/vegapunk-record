name: wt-task11
services:
  chroma:
    healthcheck:
      test:
        - CMD-SHELL
        - curl -fsS http://127.0.0.1:8000/api/v2/heartbeat || exit 1
      timeout: 5s
      interval: 10s
      retries: 5
      start_period: 10s
    image: chromadb/chroma:latest
    networks:
      default: null
    ports:
      - mode: ingress
        host_ip: 127.0.0.1
        target: 8000
        published: "8000"
        protocol: tcp
    volumes:
      - type: volume
        source: chroma-data
        target: /chroma/chroma
        volume: {}
  stella:
    build:
      context: C:\Users\oryzaes2\AppData\Local\Temp\opencode\wt-task11
      dockerfile: Dockerfile
    depends_on:
      chroma:
        condition: service_healthy
        required: true
    environment:
      CHROMA_HOST: chroma
      CHROMA_PORT: "8000"
      HOST: 127.0.0.1
      LLM_PROVIDER: mock
      PORT: "3000"
      SQLITE_PATH: /app/data/punk-records.sqlite
    healthcheck:
      test:
        - CMD-SHELL
        - bun -e "const response = await fetch('http://127.0.0.1:3000/health'); if (!response.ok) process.exit(1);"
      timeout: 5s
      interval: 10s
      retries: 5
      start_period: 10s
    networks:
      default: null
    ports:
      - mode: ingress
        host_ip: 127.0.0.1
        target: 3000
        published: "3000"
        protocol: tcp
    volumes:
      - type: volume
        source: stella-data
        target: /app/data
        volume: {}
networks:
  default:
    name: wt-task11_default
volumes:
  chroma-data:
    name: wt-task11_chroma-data
  stella-data:
    name: wt-task11_stella-data
